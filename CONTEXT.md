# 可信 RAG 银行业监管问答

本文件是开发期间的产品与技术决策记录。开始设计、拆票或开发前先阅读本文件。

- “已确认决策”是当前实现边界，变更时必须显式更新。
- “尚未确认”不得自行补全为需求。
- 原型和方案确认后，将适合公开展示的内容合并进 `docs/技术文档.md`，不在 `docs/` 长期保留重复方案文档。

## 当前实施状态

| 层级 | 当前状态 |
| --- | --- |
| 正式代码 | 已实现解析、切块、混合检索、证据约束生成、FastAPI REST/SSE、Keycloak 登录与访问控制、PostgreSQL 对话历史与滚动摘要、维护者知识库管理，以及前端批量上传编排、活动文档同名拒绝、在线上传受理、受鉴权的原件流式下载、MinIO 版本对象、Redis 单文档 Worker、Qdrant 候选写入、不可变 BM25 代际、当前版本过滤、并发安全发布、幂等重投递、过期任务恢复、候选清理、五项就绪检查和带审计的安全逻辑撤回 |
| 静态原型 | 已实现成员/维护者场景、知识库管理、上传与入库状态的视觉和前端演示；登录、账户区、问答、个人历史侧栏、维护者知识库页面和上传弹窗已接入正式 React |
| 下一阶段 | 维护者手工重试和永久物理清理尚未开发 |

文档和开发不得把静态原型或下一阶段方案描述为当前正式代码已经具备的能力。

## 已确认决策

### 产品范围

- 系统服务一个企业，不建设多企业、多租户知识库平台。
- 企业成员共同使用同一个企业共享知识库，不按用户隔离知识文档。
- 新文件可以持续上传、解析、切块并建立索引，成功后进入企业共享知识库供问答检索。
- 问答仍是产品主入口；知识库维护是受权限控制的辅助工作区。

### 公开仓库数据边界

- 可公开评测方法、汇总指标和结论，包括 `291/299`、`97.32%` 等不能还原单题内容的统计。
- 比赛 Manifest、题集、逐题答案与报告、Bad Case 回归明细和来源哈希都只能在本地私有环境保留，不进入 Git。经确认用于项目展示的 `docs/assets/` 截图允许提交。
- 公开仓库只提供源码、通用测试、空目录占位和合成示例；克隆后需使用者自行准备合法数据。

### 身份与权限

- 所有登录用户都由 Keycloak 负责认证和登录会话。
- `member`（企业成员）可以使用问答和检索。
- `knowledge_maintainer`（知识库维护者）是拥有知识库维护权限的企业成员，可以上传、更新、逻辑撤回文档并查看入库任务。
- Keycloak 系统管理员与知识库维护者不是同一业务角色。
- 后端必须校验 Keycloak 返回的身份和角色，前端隐藏按钮不能替代后端鉴权。
- 比赛展示环境不依赖真实飞书或企业微信，可在 Keycloak 中预置普通成员和知识库维护者两个演示账号；未来可以再接企业身份源。
- 公开仓库允许提供明确标注为“仅限本地演示”的固定演示凭证，便于复现完整登录流程；这些凭证不属于真实秘密，不得在任何真实环境复用。

| 能力 | 企业成员 | 知识库维护者 |
| --- | --- | --- |
| 登录、提问与检索 | 允许 | 允许 |
| 上传知识文档 | 不允许 | 允许 |
| 更新或逻辑撤回知识文档 | 不允许 | 允许 |
| 下载知识文档最新上传原件 | 不允许 | 允许 |
| 查看与重试入库任务 | 不允许 | 允许 |

### 组件职责

| 组件 | 唯一职责 | 不承担的职责 |
| --- | --- | --- |
| Keycloak | 用户认证、角色、登录会话 | 不保存业务文档和向量 |
| PostgreSQL | 用户引用、文档元数据、版本、任务记录、状态和审计记录，是业务事实来源 | 不保存密码，不代替对象存储或向量库 |
| Redis | 异步任务队列、任务协调和短期状态 | 不作为文档与任务的最终事实来源 |
| MinIO | 原始文件及其版本对象 | 不保存业务权限和检索向量 |
| Qdrant | 文档切块的向量索引 | 不保存原始文件和业务主数据 |
| BM25 索引 | 关键词检索索引 | 不作为业务主数据 |

当前正式代码已完成 PostgreSQL 连接、Alembic 迁移、可注入会话依赖和就绪检查，以及 Keycloak OIDC 登录与业务角色读取；`conversations` 与 `conversation_messages` 保存个人对话与完整消息，知识库相关表保存文档、版本、任务、版本 Chunk 工件、BM25 代际和当前发布指针，`audit_events` 记录已接受的上传和逻辑撤回操作。MinIO 保存私有原件与不可变版本 Chunk，Redis 只传递入库任务标识。

### 模型与上下文管理

- 目标模型确定为 `deepseek-v4-flash`。
- 最终公开评测使用 `deepseek-v4-flash`；当前正式代码仍通过 OpenAI 兼容接口配置模型，`.env.example`、`LLMClient` 和问题路由器的默认值已统一为 `deepseek-v4-flash`。
- 模型声明的 1M token 上下文窗口是供应商能力上限，不作为系统每次请求的默认使用量。
- 普通问答的上下文预算为 64K token；复杂问答可提升至 128K token；系统硬上限为 200K token。
- 每次调用额外为模型输出预留 8K–16K token，输入内容不得挤占输出空间。
- 上下文预算按 token 计算，不按固定消息条数计算；完整对话历史保存在 PostgreSQL 中，但每次只向模型发送当前问题所需的历史摘要、近期消息、检索证据和必要系统指令。
- DeepSeek API 按无状态调用处理；对话归属、消息持久化、历史摘要、上下文选择和裁剪均由本系统负责。
- 上述预算必须做成后端配置，后续可根据真实问答质量、延迟和成本数据调整，不写死在前端。
- 当前正式代码已在 PostgreSQL 保存完整可展示消息，并由服务端而非浏览器请求体提供当前对话历史；超出近期历史预算的旧消息会生成并持久化滚动摘要，生成与上下文化改写共用摘要与受控的最新历史。

### 文档入库一致性

- 知识库维护者可在前端单批选择最多 10 个 `.doc`、`.docx`、`.pdf`、`.xls` 或 `.xlsx` 文件，单个文件不超过 50 MiB、总计不超过 200 MiB；前端固定以 3 个并发请求复用单文件接口，每个文件独立校验、保存和创建任务，单个失败不阻断其他文件。
- 后端单文件接口校验扩展名、可识别内容、空文件和可配置大小上限（默认 50 MiB）；同批重名文件由前端标记为校验失败，知识库中已有的活动同名文档由后端拒绝，不覆盖既有文档。校验通过后保存 MinIO 私有原件，在 PostgreSQL 创建文档版本与任务，再把标识投递到 Redis。
- 后台任务完成解析、切块、向量索引和 BM25 索引后，才把新版本标记为当前有效版本。
- 入库任务只能完整成功或完整失败；处理中产生的半成品不能被企业成员检索。
- 新版本入库成功后再替换旧版本，避免更新过程中知识文档暂时不可用。
- 激活阶段使用文档级短期协调锁，并在 PostgreSQL 中锁定文档与活动 BM25 指针、核对候选开始时看到的旧状态；并发候选只能有一个成为最终当前版本。
- 每个入库任务使用任务 UUID 作为稳定幂等键，并通过 PostgreSQL 唯一约束和带令牌的短租约保证重复投递只有一个 Worker 能推进；Worker 重启后会从 PostgreSQL 重新投递排队任务和租约过期任务。
- 过期任务安全重做前按文档版本清理候选 Chunk 对象、Qdrant 点和未发布 BM25 代际；清理失败保留旧活动版本并让同一任务等待再次清理，不创建新版本或新任务。
- 入库失败对维护者只公开稳定 `result_code` 和安全中文消息，内部异常写入服务端日志；`/api/health` 只报告进程存活，`/api/ready` 汇总 PostgreSQL、Redis、MinIO、Qdrant 和 Worker 心跳。
- PostgreSQL 记录上传者、版本、状态和必要的操作审计；原始文件、向量索引和关键词索引分别由对应组件保存。
- 界面中的“删除”表示逻辑撤回：文档必须立即退出当前版本集合，后续检索不得再引用；业务元数据和审计继续保留。MinIO 原件、Qdrant 点和 BM25 代际的永久物理清理属于独立的保留与清理策略，不能用“只删原文件”代替撤回。

### 原型结构

- 原型统一放在根目录 `prototype/` 中，并以 `prototype/index.html` 作为入口，保持与当前 React 前端一致的视觉基线。
- `prototype/styles.css` 和 `prototype/app.js` 分别承载原型样式与交互；当前各只有一个文件，不额外建立单文件 `css/`、`js/` 子目录。
- 原型使用的独立静态资源放在 `prototype/assets/`，避免依赖正式前端的资源路径。
- 原型保持为视觉基准；后续工单按切片把已确认页面接入正式 React，不反向改写原型来迁就实现。
- 原型至少覆盖：登录、普通成员问答、维护者问答、知识库管理、上传与入库任务状态。
- 知识库管理采用独立页面，问答仍是默认主页。

### 原型视觉与交互

- 视觉基线沿用现有问答前端：银行蓝、深蓝正文、浅灰页面、宋体主标题与现有能力卡语言。
- 借鉴 ChatGPT 的低噪声布局、充分留白、轻边框、居中工作区和浮动输入框，但不复制其品牌元素。
- 原型中的新版问答展示已确认为后续正式前端的目标样式：银行蓝底白字的用户气泡、无外层白卡的宽幅回答、左侧可信标识，以及更轻量的证据展开区。
- 新版问答展示不要求与当前 React 回答卡保持一致；原型整体确认后，再统一修改正式前端，原型阶段不提前改 React。
- 问答工作区增加可折叠左侧栏：保留“新对话”和当前用户的最近对话；历史搜索只用一个放在侧栏右上角的搜索图标触发，不单独占一行。
- 历史对话重命名在侧栏原标题位置直接编辑；对话和知识文档删除均通过统一尺寸与风格的二次确认弹窗完成。正文分别显示 `这会删除“对话名”` 和 `这会删除《文件名》`，只加粗实际的对话名或文件名。
- 侧栏收起/展开按钮放在主内容面板左上角；侧栏收起后该按钮继续保留，用于恢复侧栏。
- “知识库管理”作为左侧栏中的固定功能入口，不混入历史对话列表；普通成员隐藏该入口，知识库维护者显示。
- 普通成员只显示问答入口；知识库维护者显示知识库管理入口。
- 知识库文档列表固定展示：序号、文件名、大小、入库状态、更新时间、操作；不在列表展示版本、发布机构或文件分类等辅助信息。正式 React 操作统一为“下载 / 删除”，不再展示详情；下载由后端鉴权后流式返回最新上传原件，进行中和失败文档同样允许下载，已撤回文档不可下载。本次不反向同步静态原型。
- 文件名使用 `《完整文件名.扩展名》` 格式；列表空间不足时以省略号截断，悬停后在当前行无底纹、无边框、无阴影地单行展开完整文件名，并临时隐藏该行的大小和入库状态以让出空间。入库状态文案统一为“成功 / 进行中 / 失败”。
- 上传使用模态框：未选文件时显示完整拖放区，选择后收为“继续添加文件”，列表逐个展示“待上传 / 正在提交 / 已受理 / 校验失败 / 提交失败”；开始上传前可逐个移除，开始后不提供取消。异步入库状态继续显示在知识库列表中。
- 知识文档列表每页固定展示 10 篇，使用完整页码、上一页和下一页；页数较多时中间页码可以折叠为省略号，搜索或切换状态后返回第 1 页。
- 原型场景控制器只用于设计检查，不属于正式产品界面。

## 尚未确认

- 企业存储配额。
- 删除后的永久保留期限。
- 入库失败的自动重试次数及人工处理方式。
- 审计日志的保留周期和展示范围。

## Language

**企业成员**：
经企业授权使用系统的人。不同企业成员使用同一企业共享知识库，不按个人隔离资料。
_Avoid_: 访客、租户

**知识库维护者**：
获准向企业共享知识库上传、更新和删除资料的企业成员。维护者与普通企业成员使用同一知识库，但拥有资料维护权限。
_Avoid_: 租户管理员、个人知识库管理员

**企业共享知识库**：
由企业持续维护、供企业成员共同检索的资料集合。新资料完成入库后成为企业统一知识的一部分。
_Avoid_: 临时知识库、个人知识库、多租户知识库

**知识文档**：
企业共享知识库中受版本管理的原始资料。只有当前有效且入库成功的版本可以参与检索。
_Avoid_: 上传文件、临时文件

**文档版本**：
同一知识文档在不同时间形成的修订。新版本成功入库后取代旧版本成为当前有效版本。
_Avoid_: 重复文件、副本

**入库任务**：
将一个文档版本转化为可检索知识的一次受跟踪处理。任务只有完整成功或完整失败，不向企业共享知识库暴露半成品。
_Avoid_: 上传请求、全量重建

**对话**：
一个企业成员连续提问与回答的独立容器。每个对话只归属于一个企业成员；新建对话会产生新的上下文边界。
_Avoid_: 企业共享会话、知识库会话

**消息**：
对话中的一次用户提问或系统回答。完整消息历史可以持久保存，但不等于每次都全部发送给模型。
_Avoid_: 问答会话、上下文窗口

**对话历史**：
当前企业成员拥有的对话列表，用于恢复、重命名和删除自己的对话。知识库维护者默认不能查看其他成员的对话历史。
_Avoid_: 企业公共历史、共享聊天记录

**模型上下文**：
系统为理解当前问题而从当前对话中选取的近期消息、历史摘要和必要事实集合。它是完整对话历史的受控子集。
_Avoid_: 全部聊天记录、左侧历史列表

**上下文预算**：
一次模型调用允许模型上下文占用的受控 token 数量。它应小于模型声明的最大上下文窗口，并为系统指令、检索证据、工具结果和模型输出预留空间。
_Avoid_: 模型最大上下文、完整 200K 历史
